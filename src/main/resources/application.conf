app{
  server {
    port = 9000
    host = "localhost"
  }
  db {
    provider = "postgres"
    driver = "org.postgresql.Driver"
    url = ""
    user = ""
    password = ""
    migrationLocation = "migration"
  }
}